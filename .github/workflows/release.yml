name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: stable

      - name: Run GoReleaser
        id: goreleaser
        continue-on-error: true
        run: |
          curl -sL "https://github.com/goreleaser/goreleaser/releases/download/v2.15.4/goreleaser_Linux_x86_64.tar.gz" -o /tmp/goreleaser.tar.gz
          tar -xzf /tmp/goreleaser.tar.gz -C /usr/local/bin goreleaser
          goreleaser release --clean 2>&1 | tee /tmp/goreleaser.log
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Upload logs on failure
        if: steps.goreleaser.outcome == 'failure'
        uses: actions/upload-artifact@v4
        with:
          name: goreleaser-logs
          path: /tmp/goreleaser.log

      - name: Fail if build failed
        if: steps.goreleaser.outcome == 'failure'
        run: |
          echo "=== GoReleaser output ==="
          cat /tmp/goreleaser.log
          exit 1
