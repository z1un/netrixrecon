name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write

jobs:
  goreleaser:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Go
        uses: actions/setup-go@v5
        with:
          go-version: stable

      - name: Test Go build
        run: go build ./...

      - name: Download GoReleaser
        run: |
          curl -sL "https://github.com/goreleaser/goreleaser/releases/download/v2.15.4/goreleaser_Linux_x86_64.tar.gz" -o /tmp/goreleaser.tar.gz
          tar -xzf /tmp/goreleaser.tar.gz -C /usr/local/bin goreleaser
          goreleaser --version

      - name: Run GoReleaser
        run: goreleaser release --clean --verbose 2>&1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
